import {{formName}}Form from '@features/{{directoryName}}/{{formFileName}}';
import PageContent from '@layouts/partials/page-content';
import PageHeader from '@layouts/partials/page-header';
import { {{modelName}}Partial } from '@models/{{modelFileName}}';

const {{createPageName}}Create = () => {
  const initialValues = {} as {{modelName}}Partial;

  return (
    <>
      <PageHeader
        title="Create {{actionTitle}}"
        subTitle="Fill in the details to create a new {{actionTitleLower}}"
      />
      <PageContent>
        <{{formName}}Form initialValues={initialValues} />
      </PageContent>
    </>
  );
};

export default {{createPageName}}Create;
