import { useParams } from 'react-router-dom';
import { Spin } from 'antd';
import {{formName}}Form from '@features/{{directoryName}}/{{formFileName}}';
import { use{{hooksName.getOne}} } from '@hooks/{{hooksFileName}}';
import PageContent from '@layouts/partials/page-content';
import PageHeader from '@layouts/partials/page-header';

const {{createPageName}}Edit = () => {
  const params = useParams();
  const {{hookDataName}}Id = Number(params.id);
  const { isLoading, {{hookDataName}} } = use{{hooksName.getOne}}({{hookDataName}}Id);

  return (
    <>
      <PageHeader
        title="Edit {{actionTitle}}"
        subTitle="Update the {{actionTitleLower}} details below"
      />
      <PageContent>
        <Spin spinning={isLoading}>
          <{{formName}}Form initialValues={ {{hookDataName}} } isEditMode />
        </Spin>
      </PageContent>
    </>
  );
};

export default {{createPageName}}Edit;
