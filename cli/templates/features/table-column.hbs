import { Link } from 'react-router-dom';
import { MenuProps, Dropdown, Button, TableProps } from 'antd';
import { MoreOutlined, EditOutlined } from '@ant-design/icons';
import TypographyWrapper from '@components/shared/typography-wrapper';
import { {{modelName}} } from '@models/{{modelFileName}}';

const getActions = (record: {{modelName}}): MenuProps['items'] => {
  return [
    {
      key: `edit-${record.id}`,
      label: <Link to={`/{{routeName}}/${record.id}`}>
        <EditOutlined /> Edit
      </Link>
    }
  ];
};

const columns: TableProps<{{modelName}}>['columns'] = [
  {
    title: 'ID',
    dataIndex: 'id',
    key: 'id',
    sorter: true,
    render: (_, record) => (
      <TypographyWrapper type="text">
        {record.id}
      </TypographyWrapper>
    )
  },
  {{#each fields}}
  {
    title: '{{title}}',
    dataIndex: '{{name}}',
    key: '{{name}}',
    render: (_, record) => (
      <TypographyWrapper type="text">
        {{fieldObject}}
      </TypographyWrapper>
    )
  },
  {{/each}}
  {
    title: 'Action',
    key: 'action',
    fixed: 'right',
    align: 'center',
    width: 100,
    render: (_, record) => (
      <Dropdown
        key={`action-${record.id}`}
        menu={{actionData}}
        trigger={['click']}
        overlayClassName="grid-action"
      >
        <Button shape="circle" icon={<MoreOutlined />} />
      </Dropdown>
    )
  }
];

export { columns };
